import { CreateDepartmentDto } from './create-department.dto';

describe('CreateDepartmentDto', () => {
  it('should be defined', () => {
    expect(new CreateDepartmentDto()).toBeDefined();
  });
});
