import { UpdateDepartmentDto } from './update-department.dto';

describe('UpdateDepartmentDto', () => {
  it('should be defined', () => {
    expect(new UpdateDepartmentDto()).toBeDefined();
  });
});
